- name: test input check subworkflow
  command: nextflow run main.nf -profile test,docker --only_input
  tags:
    - inputcheck
    - subworkflow
  files:
    - path: results/samplesheet/out.json
    - path: results/scorefile/scorefiles.pkl

- name: test input check subworkflow with PGS catalog API
  command: nextflow run main.nf -profile test,docker --accession PGS001229 --only_input
  tags:
    - inputcheck
    - subworkflow    
    - pgscatalog
  files:
    - path: results/samplesheet/out.json
    - path: results/scorefile/scorefiles.pkl
      
- name: test input check subworkflow with PGS catalog API and whitespace
  command: nextflow run main.nf -profile test,docker --accession "PGS001229, PGS000802" --only_input
  tags:
    - inputcheck
    - subworkflow    
    - pgscatalog
  files:
    - path: results/samplesheet/out.json
    - path: results/scorefile/scorefiles.pkl
